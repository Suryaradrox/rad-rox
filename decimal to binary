
#include <stdio.h>

int main()
{
    int dec,i=0,j,arr[32]={0};
    printf("give value of decimal number");
    scanf("%d",&dec);
    while(dec>0)
{
    arr[i]=dec%2;
    dec=dec/2;
    i++;
}
for(j=i-1;j>=0;j--)
printf("%d",arr[j]);
    return 0;
}
